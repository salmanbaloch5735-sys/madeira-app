name: Build Unsigned IPA for SideStore on: workflow_dispatch: # Gives you a manual "Run" button jobs: build: runs-on: macos-latest steps: - name: Checkout Source Code uses: actions/checkout@v4 - name: Set up Xcode uses: maxim-lobanov/setup-xcode@v1 wit...
